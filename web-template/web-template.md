# Web Template

This HTML consit of a simple web template to run the client with assets.epk and classes.js. No code should be modified. If you wish to modify the HTML code, modify to your hearts desire. It's preferred to keep the local storage code to keep it from not saving client settings. 